import { StyleSheet, Dimensions } from 'react-native';

export const adminStyles = StyleSheet.create({

    headerContainer: {
        marginBottom: 40,
        alignItems: 'center',
      },
    logo: {
    width: 300,
    height: 300,
    marginBottom: 20,
    },


});
